import type { SiteConfig } from "@spin-clinic/ui";

export const site: SiteConfig = {
  name: "spin.clinic",
  domain: "spin.clinic",
  tagline: "Źródła, kontekst i Twoje media",
  primaryColor: "#7e263c",
};
