"use client";
import { useQuery } from "@tanstack/react-query";
import { apiFetch } from "../lib/api";
type ArchiveStatus = { records: number; active_sources: number; worker_status: "running" | "unconfirmed"; checked_at: string; complete: false };
export function ArchiveProgress() { const status = useQuery({ queryKey: ["archive-progress"], queryFn: () => apiFetch<ArchiveStatus>("/api/archive/status/"), staleTime: 10_000, refetchInterval: 30_000, retry: false }); const data = status.data; return <section aria-label="Rozbudowa archiwum" className="sc-archive-progress"><div><p className="sc-t-title-s">Budujemy archiwum polskich źródeł.</p><p className="sc-t-body sc-text-2">Uzupełniamy bieżące doniesienia i materiały z przeszłości. Pokazujemy także znane braki.</p></div><div className="sc-archive-progress__stats sc-t-meta"><p>{data ? <><strong>{data.records.toLocaleString("pl-PL")}</strong> materiałów · {data.active_sources} aktywnych źródeł</> : "Odczyt stanu archiwum…"}</p><p>{status.isError ? "Stan importerów chwilowo niedostępny" : data?.worker_status === "running" ? "Importery działają · archiwum jest rozbudowywane" : data ? "Brak bieżącego potwierdzenia pracy importerów" : "Sprawdzam pracę importerów…"}</p></div></section>; }
